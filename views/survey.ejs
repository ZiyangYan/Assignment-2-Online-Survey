<%= survey.name %>,<%= survey.cities %>,<%= survey.foods %>,<%= survey.cartoons %>,<%= survey.animals %>

                    <!-- <form action="/surveys/delete/<%= survey.$loki %>" method="POST">
    <button type="submit">Delete</button>
</form> -->
                    <button type="button" class="button is-danger"
                        onclick="deleteBooking('<%= survey.$loki %>')">Delete</button>

                    <script>
                        async function deleteBooking(id) {
                            var r = confirm("Confirm Delete?");

                            if (r) {
                                var response = await fetch("/surveys/" + id, {
                                    method: "DELETE",
                                });

                                if (response.ok) {
                                    // var html = await response.text();
                                    // alert(html);
                                    alert("survey deleted.");
                                    location.assign("/surveys")
                                } else {
                                    alert(response.status + ": " + response.statusText);
                                }

                            } else {
                                alert("cancelled");
                            }
                        };
                    </script>